# Change the folder viewing option to file extenstions

# Install git bash before running install.sh git bash: 
- https://gitforwindows.org/
- select use git in gitbash
- use unix style
- use mintty
- change the starting path to PythonScript
- change cursor to block and no blinking
- under keys, select ctrl+shift shortcuts

# install all packages (use admin rights)
./install.sh

# after installation, remember to restart the computer for NI packages
# for updating the environment:
conda env update -f local.yml